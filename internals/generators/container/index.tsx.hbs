/*
 * {{properCase name}}
 */
 
import { S{{properCase name}} } from './styles';
{{#if reducer}}
import { {{camelCase name}}Reducer } from './store';
import useInjectReducer from 'redux/useInjectReducer';
{{/if}}

interface Props {

}

export default function {{properCase name}} (props: Props) {
  {{#if reducer}}
  useInjectReducer('{{ camelCase name }}', {{camelCase name}}Reducer);
  {{/if}}

  return (
    <S{{properCase name}}></S{{properCase name}}>
  )
}
