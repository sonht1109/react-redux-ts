export interface CheckboxProps {
  checked?: boolean;
  activeColor?: string;
  color?: string;
  size?: number;
  borderWidth?: number;
  onChange?: (val: boolean) => void;
  borderRadius?: number;
}
